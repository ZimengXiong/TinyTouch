name: tinytouch-api
services:
  api:
    build: .
    container_name: tinytouch-api
    restart: unless-stopped
    volumes:
      - tinytouch-data:/data
    networks: [proxy]
volumes: {tinytouch-data: {}}
networks: {proxy: {external: true}}
